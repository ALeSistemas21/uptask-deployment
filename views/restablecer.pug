doctype html
html(lang="en")
    include partials/header 
    body.login
        .contenedor-formulario 
            h1= nombrePagina
           
            if locals.mensajes  
            .alerta 
                each categoria in Object.keys(locals.mensajes) 
                    each error in mensajes [categoria]
                        .alerta(class=`${categoria}`)
                            p= error

            p ingresa tu e-mail para restablecer la contraseña
                        
                form.caja-login(method='POST' )
                    .campo 
                        label(for='email') E-mail: 
                        input(type="email" name='email' placeHolder='Tu email' value= email required)
                                                            
                    .campo.enviar 
                        input.boton(type='submit' value='Restablecer password')

                    .campo.acciones 
                    a(href="/iniciar-sesion") Volver al login
